global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'mqtt_metrics_collector'
    scrape_interval: 5s
    static_configs:
    - targets: ['mqtt_metrics_collector:8111']

# uncomment to provide remote target url and credentials
#remote_write:
# - url: <PROM_REMOTE_PUSH_URL>
#    basic_auth:
#      username: <PROM_REMOTE_USERNAME>
#      password: <PROM_REMOTE_PASSWORD>